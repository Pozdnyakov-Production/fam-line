{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd server && npm install",
    "rootDirectory": "server"
  },
  "deploy": {
    "startCommand": "node server.js",
    "rootDirectory": "server"
  }
}